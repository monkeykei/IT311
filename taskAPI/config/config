<?php
$host = 'localhost';
$db_name = 'todo_db';
$username = 'root';
$password = '';

// Create connection
$connection = mysqli_connect($host, $username, $password, $db_name);

// Check connection
if (!$connection) {
    die("Connection failed: " . mysqli_connect_error());
}
?>